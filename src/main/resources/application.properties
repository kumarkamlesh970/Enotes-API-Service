spring.application.name=Enotes-API-Service
# = Server Configuration
server.port=8080
# server.error.include-message=alwaysS
# = Database Configuration

spring.datasource.url=jdbc:mysql://localhost:3306/enote
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#  JPA / Hibernate Configuration

spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# = Logging Configuration =
#logging.level.org.springframework=INFO
# logging.level.com.enotes=DEBUG

# = Swagger Configuration (if used) =
# springdoc.api-docs.enabled=true
# springdoc.swagger-ui.enabled=true
# = File Upload/Download (optional) =

